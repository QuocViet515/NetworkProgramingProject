    <UserControl x:Class="Pingme.Views.Pages.ChatPage"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:local="clr-namespace:Pingme.Models"
                 xmlns:controls="clr-namespace:Pingme.Views.Controls"
                 xmlns:vm="clr-namespace:Pingme.ViewModels"
                 xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

        <UserControl.DataContext>
            <vm:ChatViewModel/>
        </UserControl.DataContext>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <controls:Sidebar Grid.Column="0"/>

            <!-- Chat area -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Tên người dùng -->
                <TextBlock Text="{Binding SelectedUser.fullName}"
                           FontWeight="Bold"
                           FontSize="18"
                           Foreground="White"
                           Margin="10"
                           HorizontalAlignment="Center"
                           Grid.Row="0"/>

                <!-- Tin nhắn -->
                <controls:MessageList Grid.Row="1"/>

                <!-- Thanh input -->
            <controls:MessageInputBar Grid.Row="2" Margin="-10,5,10,-5" />
        </Grid>
        </Grid>
    </UserControl>
