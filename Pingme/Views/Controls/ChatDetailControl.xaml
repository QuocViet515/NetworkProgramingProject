<UserControl x:Class="Pingme.Views.Controls.ChatDetailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pingme.Views.Controls"
             xmlns:controls="clr-namespace:Pingme.Views.Controls">
    <Border CornerRadius="10" Padding="5" Margin="10">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                <GradientStop Color="#C7FADF" Offset="0.0"/>
                <GradientStop Color="#94B9FF" Offset="1.0"/>
            </LinearGradientBrush>
        </Border.Background>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Thanh tiêu đề -->
            <controls:ChatHeader ContactName="Group 10 (NT106.P21.ANTT)" AvatarPath="/Assets/Icons/avatar-default.png"/>

            <!-- Danh sách tin nhắn -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden" PanningMode="VerticalOnly" CanContentScroll="True">
                <StackPanel Name="ChatPanel" Margin="10">
                    <controls:ChatSeparator DateText="11 / 3 / 2025"/>
                    <controls:MessageChat AvatarPath="/Assets/Icons/avatar-default.png" Message="Chào mọi người"/>
                    <controls:MessageChat AvatarPath="/Assets/Icons/avatar-default.png" Message="Chào các bạn"/>
                    <controls:UserChat AvatarPath="/Assets/Icons/avatar-default.png" Message="Giờ mình phân công công việc nha"/>
                    <controls:MessageChat AvatarPath="/Assets/Icons/avatar-default.png" Message="Oke bạn"/>
                    <controls:UserChat AvatarPath="/Assets/Icons/avatar-default.png" Message="Mọi người vào nhận công việc nha"/>
                    <controls:ChatSeparator DateText="28 / 3 / 2025"/>
                    <controls:SystemNotification AvatarPath="/Assets/Icons/avatar-default.png" Message="Đã rời khỏi nhóm"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Khung nhập tin nhắn -->
            <Border Grid.Row="2" Background="#FFFFFF" CornerRadius="10" Padding="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Margin="4,0,0,0" Background="Transparent" BorderThickness="0" ToolTip="Gửi">
                        <Image Source="/Assets/Icons/foldericon.png" Width="50" Height="42"/>
                    </Button>

                    <Border Grid.Column="1" Background="#E1E1E1" CornerRadius="10" VerticalAlignment="Center" Margin="10,0">
                        <TextBox Name="MessageInput" Height="40" Margin="10,0" Padding="5"
                                 Background="Transparent" BorderThickness="0"
                                 FontWeight="Bold" FontSize="16" Foreground="Black"
                                 VerticalContentAlignment="Center" HorizontalContentAlignment="Left"
                                 Text="Nhắn gì đó . . ." />
                    </Border>

                    <Button Grid.Column="2" Margin="5,5,2,3" Background="Transparent" BorderThickness="0" ToolTip="Gửi">
                        <Image Source="/Assets/Icons/sendicon.png" Width="51" Height="50"/>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
