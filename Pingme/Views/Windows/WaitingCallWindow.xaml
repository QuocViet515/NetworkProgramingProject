<Window x:Class="Pingme.Views.Windows.WaitingCallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Đang gọi..."
        Height="400"
        Width="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FAFAFA"
        WindowStyle="None"
        AllowsTransparency="True">
    <Border CornerRadius="12" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Padding="20" >

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >

            <!-- Avatar -->
            <Ellipse Width="100" Height="100" Margin="0,10" Stroke="#CCCCCC" StrokeThickness="1">
                <Ellipse.Fill>
                    <ImageBrush ImageSource="/Assets/Icons/avatar-default.png"/>
                </Ellipse.Fill>
            </Ellipse>

            <!-- Status Text -->
            <TextBlock Text="Đang chờ người nhận bắt máy..."
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="#333"
                       Margin="0,10"
                       TextAlignment="Center"/>

            <!-- Loading ring -->
            <Viewbox Width="40" Height="40" Margin="0,10">
                <Grid>
                    <Ellipse Stroke="#E0E0E0" StrokeThickness="4"/>
                    <Ellipse Stroke="#2196F3" StrokeThickness="4"
                             StrokeDashArray="2"
                             StrokeDashCap="Round">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="Spinner" Angle="0" CenterX="20" CenterY="20"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Ellipse.Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="Spinner"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0" To="360"
                                                         Duration="0:0:1.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>
            </Viewbox>

            <!-- Cancel button -->
            <Button Content="❌ Hủy cuộc gọi"
                    Width="150"
                    Height="35"
                    Background="#F44336"
                    Foreground="White"
                    FontWeight="Bold"
                    Margin="0,20,0,0"
                    BorderBrush="#D32F2F"
                    Click="CancelCall_Click"
                    Cursor="Hand"
                    BorderThickness="1"
                    HorizontalAlignment="Center"
                    />

        </StackPanel>
    </Border>
</Window>
